import { CLUB_REDUCER_INIT } from './initialStates';

export default function clubReducer(state = CLUB_REDUCER_INIT, action) {

    switch (action.type) {

        default:

            return state;            
    }
}