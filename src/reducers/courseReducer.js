import { COURSE_REDUCER_INIT } from './initialStates';

export default function courseReducer(state = COURSE_REDUCER_INIT, action) {

    switch (action.type) {

        default:

            return state;            
    }
}