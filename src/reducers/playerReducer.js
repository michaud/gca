import { PLAYER_REDUCER_INIT } from './initialStates';

export default function playerReducer(state = PLAYER_REDUCER_INIT, action) {

    switch (action.type) {

        default:

            return state;            
    }
}
